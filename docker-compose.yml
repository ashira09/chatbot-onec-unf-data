services:
  python:
    build: 
      dockerfile: ./docker/python/Dockerfile
      context: ./
    container_name: python
    restart: unless-stopped
    volumes:
      - "./src:/app/src" 
      - ".env:/app/.env"